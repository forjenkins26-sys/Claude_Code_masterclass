# Nested project with its own git repo + remote — tracked separately
SeleniumRepo/

# Local Claude Code state / session artifacts
.claude/settings.local.json
.playwright-mcp/

# Build artifacts
*.class

# Node
node_modules/

# OS
.DS_Store

# One-off generated session-recap HTML
919a6e828f599e85.html

# Secrets — never commit (CLAUDE.md hard rule)
.env
**/.env
.vercel/
**/.vercel/

# ── Demo / client E2E projects — LOCAL ONLY, never pushed ──────────────────
# These are disposable per-Epic working folders. Only workspace-level assets
# (skills, rulebooks, qa-ai-stack, scripts/) belong in this repo.
# Files stay on disk; git simply ignores them.
ClientDemo-*/
BlinkitDemo-*/
QuickDemo*/
*Demo*/
Blinkit-Demo/
MediCare-QA-Demo/
*-QA-Demo/
*-E2E/

# --- RAG (added 2026-08-30) ---
# .env.bak-* holds real keys and is NOT covered by the **/.env rule above
**/.env.bak-*
**/.env.*.local
# Python virtualenv + local vector store: huge, machine-specific, never in git
RAG/**/.venv/
RAG/**/chroma_db/
RAG/**/__pycache__/
RAG/**/node_modules/
RAG/**/dist/

# --- push-everything pass (2026-08-30) ---
# Nested git repos: `git add` would store these as empty gitlinks, so a clone
# gets empty folders. They have their own remotes - push them there instead.
/Advance-Playwright-Framework/
/agent-factory-cli/
/agent-skills/
/andrej-karpathy-skills/
/qa-assistant/
/qabuddy/
/RAG/Basic_Rag/app/
# Local worktrees + backups: machine-specific, 200MB+
/.claude/worktrees/
.claude/settings.json.bak-*
# Installed applications: multi-GB, reinstallable from npm/pip, GitHub rejects this size
/langflow/
/n8n-local/
# Playwright DOM snapshot that captured a Pinecone credential modal with the key visible
/before-save.yml
