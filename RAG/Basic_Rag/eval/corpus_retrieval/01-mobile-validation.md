# Mobile Number Validation

The mobile number field accepts exactly 10 digits.
Submitting fewer than 10 digits shows the error "Enter valid 10-digit mobile number".
Non-numeric characters are rejected; letters typed into the field do not appear.
The field enforces a maxlength of 10 at the browser level.
