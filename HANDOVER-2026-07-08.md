# Handover — QA-AI Stack Session (2026-07-08)

**Purpose:** resume work in a fresh session with full context. Read this first. (Older handover: `HANDOVER-QA-FRAMEWORK.md` from 2026-06-23.)

---

## 1. What this workspace is

`c:\ClaudeCodeMasterclass` — QA automation masterclass. Crown jewel = a **portable agentic QA stack** (`qa-ai-stack/`), also pushed as a standalone clone-able repo. Full 3-step E2E flow: `/explore` (live DOM→POM) → `/test-case-creation` (Epic AC→test cases) → `/test-case-execution` (run headed→classify→auto-fix or file real Jira bug).

## 2. The two repos (keep straight)

| Repo | Remote | Role |
|---|---|---|
| **Masterclass** | `github.com/forjenkins26-sys/Claude_Code_masterclass.git` (origin) | full workspace; personal |
| **Stack (standalone)** | `github.com/forjenkins26-sys/AI_E2E_QA_Automation_Playwright_Master_Framework.git` (remote `qa-stack`) | portable QA brain; clone at a new office |

**Current tips (VERIFY with `git ls-remote`):** stack `96a20aa`, masterclass `a7c9b30`.

**Push pattern for the stack folder** (it's a subfolder of masterclass): recent method = clone stack repo separately to `~/qa-ai-stack`, edit there, `git push origin main`, then mirror files back into `c:\ClaudeCodeMasterclass\qa-ai-stack\` and commit to masterclass. Keep both in sync. (Alt: `git subtree split --prefix=qa-ai-stack -b tmp` then `git push qa-stack tmp:main --force-with-lease`.)

## 3. Stack contents (current, verified)

- **10 skills** (`~/.claude/skills/` + `qa-ai-stack/skills/`): explore, test-case-creation, test-case-execution, bug-triage, create-bug, playwright-ai-mcp-tutor, karpathy-guidelines, qa-run, spec-quality, guard
- **ANTI-HALLUCINATION-RULES.md — 30 rules** (root + stack IDENTICAL, in sync; Rule 30 = "recalled memory is a claim, not a fact — re-verify before use", closes KB feedback-loop risk)
- **AUTO-FIX-PROTOCOL.md — 17 rules** (Rule 16 surgical, Rule 17 independent maker≠checker verify)
- **Knowledge Base** (bug-vs-intended oracle, BR-xx, Confirmed/Suspected tiers) — the standout differentiator
- **agent-factory** (RCA/Heal/Flaky/Triage), rule-engine (optional selector gatekeeper), LLM-as-a-Judge
- **`/guard`** — local `.git/info/exclude` + pre-commit hook so stack never pushes to a company repo. Re-run per machine/clone. CLAUDE.md has a FIRST-ACTION auto-trigger.

## 4. Hard rules the assistant MUST follow (learned this session)

1. **No flip-flopping.** One firm position, hold it; user pushback ≠ reverse. (memory: `feedback_no_flipflop.md`)
2. **Guideline 5 (karpathy):** an external repo having a feature we lack is NOT a gap — a gap is OUR pain. Adopt only what solves a felt problem. Separate "more CAPABLE" (do it) from "just tidier" (skip).
3. **Never hallucinate:** verify with tools (read file, run check), never claim from memory. Rule 30 applies to the assistant too.
4. **Do NOT push to qa-stack without explicit user approval.**
5. **Don't modify the 3 core skills' logic** without a real reason.
6. **BLAST protocol:** update progress.md / rca-log.md on build tasks.

## 5. Session decisions log (adopted vs skipped)

| Source | Verdict |
|---|---|
| Vara Prasad 7-agent | ADOPTED → `/qa-run` + `/spec-quality` |
| cobusgreyling loop-engineering | ADOPTED → AUTO-FIX Rule 17 (maker/checker) |
| Himanshu infographic | INSPIRED → AH Rule 30 (memory re-verify) |
| mvsaran agent-e2e | selector gatekeeper → OPTIONAL in stack config, NOT applied to workspace frameworks (reverted; not our pain) |
| prkhar cli-agents | SKIP — subagent split + modular refs = tidier not more capable |
| QA-FORGE (ankit) | SKIP mostly; only possible gap = XLSX export (banking wants Excel) — undecided |
| Vibium / agent-browser / Graphify / halovivek / Malempati / agentic-os content tool | SKIP — not QA pain or not a real tool |

## 6. OPEN ITEMS — pick up here

### A. VAPT security skill — DECISION PENDING (most recent, top priority)
- **`web-infra-vapt-skill`** by Suryam2498 — a 186-line methodology SKILL.md (VAPT: recon→enum→web→infra→attack-chains→OWASP/CVSS report). VERIFIED SAFE (pure doc, no auto-exec, no secrets, Rule-0 authorization-first). GitHub: `github.com/Suryam2498/web-infra-vapt-skill`. Registry `qaskills.sh` (CLI `npx @qaskills/cli add web-infra-vapt-skill` needs a real TTY — failed in non-interactive shell; installed content read via GitHub instead).
- **Assessment:** genuinely useful — fits banking/insurance domain + real career value (VAPT/OWASP/DAST). Best external find of the session.
- **NOT installed, NOT pushed** — awaiting user approval.
- **If approved:** add SKILL.md to `~/.claude/skills/web-infra-vapt/` (local). Attach ABSOLUTE rule: practice ONLY on legal targets (OWASP Juice Shop, DVWA, scanme.nmap.org, own demo apps) — NEVER company/client/third-party (illegal + career-ending in banking).

### B. Resume — 2 tiny fixes left (user was iterating; strong ~9.2/10)
1. **Barclays** — summary lists it as a client but there is NO Barclays project in work experience. Real or remove (only interview risk).
2. **29 → 30 rules** — resume says "Anti-Hallucination Protocol (29 rules)" in ~3 spots; actual is now 30. Bump.
- RAG already removed (false claim — user builds context-grounding, NOT vector RAG). Do NOT re-add RAG.
- Resume-assistant handover: `Downloads/RESUME-UPDATE-HANDOVER.md`.

### C. Optional (only if user asks)
- XLSX export for test-case-creation (QA-FORGE idea; banking wants Excel) — user hasn't decided if it's their pain.

## 7. Key file locations

- Stack: `c:\ClaudeCodeMasterclass\qa-ai-stack\` (+ live skills `~/.claude/skills/`)
- Rules: `ANTI-HALLUCINATION-RULES.md` (root + qa-ai-stack, identical, 30 rules), `AUTO-FIX-PROTOCOL.md` (17 rules)
- Anti-hallucination one-pager artifact: `Downloads/QA-Hallucination-Defense.html`
- Resume handover: `Downloads/RESUME-UPDATE-HANDOVER.md`
- Memory: `~/.claude/projects/c--ClaudeCodeMasterclass/memory/` (MEMORY.md index + feedback files)

## 8. First actions for the new session

1. Read this file + `CLAUDE.md` + `MEMORY.md`.
2. `git ls-remote qa-stack main` + `git ls-remote origin main` — confirm tips match section 2 (else note drift).
3. Ask which open item to pick up: **(A) VAPT skill install**, **(B) resume Barclays + 29→30**, or something new.
4. Hold Guideline 5 + no-flip-flop. Don't push to qa-stack without approval.
