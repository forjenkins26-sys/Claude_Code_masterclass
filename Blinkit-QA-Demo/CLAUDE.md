# CLAUDE.md — Blinkit-QA-Demo

> Law for this project. This file is truth.

## What this is

A clean E2E QA project for a **live client demonstration** of the AI-powered QA framework. Nothing has been run yet — the demo executes the full pipeline in front of the client, live.

## App Under Test

| | |
|---|---|
| **URL** | https://blinkit-demo-qa.vercel.app |
| **App** | Blinkit — grocery delivery, customer login |
| **Hosting** | Vercel, public, no auth wall |
| **Jira project** | `SCRUM` on `anandsoni2641.atlassian.net` |
| **Epic** | **SCRUM-603** — Blinkit — Customer Login Quality Assurance (14 ACs) |

Other pages on the same deployment (out of scope for this Epic — separate `/explore` runs):
- `/blinkit-products.html` — catalogue, search, cart drawer
- `/blinkit-checkout.html` — checkout

The app is public, so `/explore` uses **live Playwright MCP**, not `scripts/fetch-local-page.js`. That script is present only as a localhost fallback.

## Demo Flow (the 4-step story)

| # | Command | What the client sees | Takes |
|---|---|---|---|
| 1 | `/explore https://blinkit-demo-qa.vercel.app` | Live browser opens, DOM read, Page Object generated from real elements | ~2 min |
| 2 | `/test-case-creation SCRUM-603` | Requirements → test cases in Jira, edge-case matrix walked per control | ~4 min |
| 3 | `/test-case-execution SCRUM-603` | Headed run. **Failures classified before any fix.** Real bugs filed, test issues auto-fixed | ~5 min |
| 4 | `/test-closure SCRUM-603` | AC→test traceability, counted coverage, Go / No-Go verdict | ~2 min |

Optional 5th: `/requirement-drift SCRUM-603` — edit an AC in Jira mid-demo, then show it detecting the drift and scoping the re-test.

## The moment that sells it

Step 3. Two failures, two different verdicts:

- **A real defect** → classified `REAL_BUG`, cited against a `BR-xx` rule, filed in Jira, test marked blocked. **The framework refuses to "fix" it.**
- **A test's own fault** → classified `TEST`, auto-fixed, re-verified headed, no bug filed.

Say this out loud: *"Normal AI would have written a test that agrees with the bug and turned the suite green. This one read the requirement first, so it knew the difference."*

## Hard Rules (always active)

1. **Headed mode mandatory** — `headless: false` in config. Never flip it (AH Rule 17)
2. **The requirement is the oracle** — expected behaviour comes from the Epic AC, never from what the UI shows (AH Rule 19)
3. **Classify before fixing** — no auto-fix until the failure is categorised (AH Rule 23)
4. **Never fix a test that caught a real bug** — file the defect, mark the test blocked
5. **Capture the build every run** — a changed build expires all prior results (Step 0B)
6. **Counted coverage only** — `n/m`, never estimated. No execution block = 0% coverage, not 100%
7. **A missing AC is a finding**, never a blank to fill by invention
8. **`RUN_ID` per run** — `test-results/` is wiped on every invocation (AH Rule 31)
9. **URL scope** — this Epic covers the login page only. A click that navigates to the catalogue gets **one** navigation assertion; the catalogue's contents are a separate run (AH Rule 27)

## Knowledge Base

```
knowledge-base/SCRUM/
  business-rules.md    ← bug oracle (BR-xx). Seed from the Epic ACs before Step 3
  known-defects.md     ← dedup register — check before filing
  feature-map.md       ← regression blast radius
  product-flows.md     ← user journeys
  app-patterns.json    ← how the app is BUILT — hint only, never an oracle
```

**Seed `business-rules.md` from the Epic ACs during Step 2.** Without it, Step 3 loses Confirmed/Suspected bug tiering and dedup — and it fails *silently*, which is the failure mode to avoid.

**Dedup warning:** the workspace KB already carries Blinkit defects from earlier Epics (SCRUM-121, SCRUM-386, SCRUM-403). This project has its own fresh KB. If a defect here matches one already filed against a previous Blinkit Epic, reference it rather than filing a duplicate — check `known-defects.md` and JQL before creating (AH Rule 21).

## Commands

```bash
npm install                    # first time only
npx playwright install         # first time only

npm test                       # all tests (headed per config)
npm run test:headed            # explicit headed
npm run report                 # open the HTML report
npm run typecheck              # TypeScript check

RUN_ID=demo-1 npx playwright test    # named run — keeps artefacts separate
```

## Layout

```
src/pages/          ← Page Objects, generated by /explore
src/fixtures/       ← fixture DI
tests/ui/           ← specs, generated by /test-case-creation
knowledge-base/     ← 5-file product memory
scripts/            ← fetch-local-page.js (localhost fallback only)
output/             ← closure reports
screenshots/{EPIC}/ ← evidence, PASS and FAIL
```

## Skills used

All live globally at `~/.claude/skills/` and travel to every project — **never copied per project**.

`/explore` · `/test-case-creation` · `/test-case-execution` · `/test-closure` · `/requirement-drift` · `/qa-run` (chains 1-3) · `/spec-quality` · `/bug-triage`

## BLAST logging

Every skill run appends to the workspace-root `progress.md` at `c:\ClaudeCodeMasterclass\progress.md` — not to this project. That is the single execution log across all projects.
